type Query {
    list: [Image]
}

type Image  {
    id: ID
    name: String
    urlPath: String
}

enum Role {
    ADMINISTRATOR
    VIEWER
}

type User {
    id: ID
    login: String
    password: String
    role: Role
}